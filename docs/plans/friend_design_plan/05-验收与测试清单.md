---
status: proposed
scope: 好友、同屏、跨 Actor Saga 的最小验收
---

# 好友功能验收与测试清单

## 实施前评审结论

总体业务方向合理，与当前 Player Actor、双 Zone 路由和 Tcaplus 单记录
CAS 模型兼容。2026-08-05 已确认以下实施决策：

1. 完成游戏内部 gRPC 改造：迁移 Gate → Zone 命令和 Zone → Gate Push，
   新增 FriendSvr、Zone → Zone RPC；Login 与 Coordinator HTTP 本期不改。
2. `FriendRelation(min_player_id, max_player_id)` 是唯一权威关系；
   `FriendList` 是可修复查询投影，并持久化按 `link_id` 区分的 reservation。
3. `FriendCodeCurrent` 以 `owner_player_id` 为主键保证一人一个有效代码，
   `FriendCodeLookup` 提供按代码查找。
4. 访客 reservation/receipt 与 Owner applied receipt 进入各自 Player
   Checkpoint；`FriendInteraction` 只记录编排进度。
5. `farm_view_epoch` 在 Owner Actor 激活和 Shard 迁移后变化。
6. 内部 gRPC 最小原型使用 timestamp + nonce + body digest 的 HMAC
   Metadata，并按 caller service 授权。
7. 第二章包含三个任务：双方成功建立好友关系各推进一次、偷菜一次、
   向好友地块投虫一次。

下面的验收项是开发门禁。

## 1. FriendSvr

- [x] 好友代码创建后 10 分钟内可被多个不同玩家兑换；
- [x] 代码过期后拒绝兑换，之后才允许创建新代码；
- [x] 自己不能兑换自己的代码；
- [x] 同一人重复兑换不重复占名额；
- [x] 每人第 101 个好友被拒绝；
- [x] 并发兑换不会让任一方超过 100 人，失败/中断 reservation 可对账释放；
- [x] FriendLinkSaga 在任一步中断后不会暴露可访问的单向好友；
- [x] 伪造调用者身份或绕过 Saga 状态不能通过 `CheckMutualFriend`；
- [x] 好友列表返回最多 100 个 `account_name`。
- [x] `FriendRelation` 激活后双方“加好友”任务各推进一次，重试不重复推进。

说明：以上条目由 `server/internal/friend` 与
`server/internal/player/social_task_credit_test.go` 单测覆盖；Gate WS
好友 action 路由尚未接线，不影响 FriendSvr 本身验收。

### 预计涉及文件

新增：

- `proto/classicfarm/v1/friend/friend.proto`
- `proto/classicfarm/v1/rpc/runtime.proto`
- `deploy/tcaplus/schema/classicfarm/v1/tcaplus/friend_tables.proto`
- `server/internal/friend/service.go`
- `server/internal/friend/store.go`
- `server/internal/friend/tcaplus_store.go`
- `server/internal/friend/link_saga.go`
- `server/internal/friend/reconciler.go`
- `server/internal/friend/service_test.go`
- `server/internal/friend/tcaplus_store_test.go`
- `server/internal/friend/link_saga_test.go`
- `server/cmd/friend/main.go`
- `server/internal/platform/rpcauth/interceptor.go`
- `server/internal/platform/rpcauth/interceptor_test.go`
- `server/internal/gateway/grpc_adapters.go`
- `server/internal/gateway/grpc_adapters_test.go`
- `server/cmd/zone/grpc_server.go`
- `server/cmd/zone/grpc_server_test.go`
- `server/internal/player/social_task_credit.go`
- `server/internal/player/social_task_credit_test.go`
- `deploy/k8s/friend.yaml`

修改：

- `buf.gen.yaml`
- `server/go.mod`
- `server/go.sum`
- `Dockerfile`
- `.env.example`
- `start-servers.sh`
- `proto/classicfarm/v1/ws/ws.proto`
- `proto/classicfarm/v1/data/data_model.proto`
- `server/cmd/gate/main.go`
- `server/internal/gateway/gateway.go`
- `server/internal/gateway/http_adapters.go`
- `server/internal/player/state.go`
- `server/internal/player/checkpoint.go`
- `server/internal/player/runtime.go`
- `server/internal/player/config.go`
- `server/internal/testtcaplus/client.go`
- `tests/catalog.json`
- `deploy/k8s/configmap.yaml`
- `deploy/k8s/secret.example.yaml`
- `deploy/k8s/services.yaml`
- `deploy/k8s/kustomization.yaml`
- `deploy/k8s/README.zh-CN.md`
- `deploy/tcaplus/README.zh-CN.md`
- `docs/contracts/websocket-protocol.md`
- 生成目录：`server/gen/classicfarm/v1/friend/`
- 生成目录：`server/gen/classicfarm/v1/rpc/`
- 生成目录：`server/gen/classicfarm/v1/tcaplus/`
- 生成目录：`web/src/gen/classicfarm/v1/friend/`

## 2. 访问与心跳

- [x] H5 一次只能访问一座好友农场，切换自动退出旧农场；
- [x] 非双向好友不能 `ENTER_FARM`；
- [x] 成功进入返回公开快照和 `visit_id`；
- [x] 30 秒心跳刷新访客表；
- [x] 90 秒无心跳清除访客；
- [x] `EXIT_FARM` 立即清除访客；
- [x] Owner Zone 重启后旧 `visit_id` 被拒绝，H5 自动重新进入；
- [x] Owner Shard 迁移后旧 `visit_id` 和旧公开视图 epoch 被拒绝；
- [x] 伪造 `caller_player_id`、`owner_player_id` 或 `gate_id` 的内部调用被拒绝；
- [x] 农场主收到访客进入/离开的短暂提示。

说明：以上条目由 `server/internal/visit`（registry/service/owner_service 单测）
与 `server/cmd/zone/friend_rpc_test.go`（RPC 参数校验、伪造身份、Shard
迁移拒绝）覆盖服务端行为；H5 侧一次一座农场、30 秒心跳、
`VISIT_NOT_FOUND` 自动重新进入、访客进入/离开提示在
`web/src/App.vue` + `web/src/lib/ws.ts` 中实现，未写自动化 E2E（本阶段
以人工验证 + `npm run build` 类型检查为主）。

### 预计涉及文件

新增：

- `server/internal/visit/registry.go`
- `server/internal/visit/registry_test.go`
- `server/internal/visit/service.go`
- `server/internal/visit/service_test.go`
- `server/internal/visit/owner_client.go`
- `server/cmd/zone/friend_rpc.go`
- `server/cmd/zone/friend_rpc_test.go`

修改：

- `proto/classicfarm/v1/ws/ws.proto`
- `proto/classicfarm/v1/friend/friend.proto`
- `server/cmd/zone/main.go`
- `server/cmd/gate/main.go`
- `server/internal/gateway/gateway.go`
- `server/internal/gateway/http_adapters.go`
- `server/internal/routing/http_client.go`
- `server/internal/platform/rpcauth/interceptor.go`
- `web/src/lib/ws.ts`
- `web/src/main.ts`
- `docs/contracts/websocket-protocol.md`
- 生成目录：`server/gen/classicfarm/v1/ws/`
- 生成目录：`server/gen/classicfarm/v1/friend/`
- 生成目录：`web/src/gen/classicfarm/v1/ws/`

## 3. 公开同步

- [x] 公开视图不包含金币、背包、任务和行动次数；
- [x] 农场主和多个访客收到相同公开地块变化；
- [x] 私有买种等变化不增加 `farm_view_seq`；
- [x] 公开地块变化才增加 `farm_view_seq`；
- [x] `farm_view_epoch` 改变后 H5 用完整快照替换旧视图；
- [x] 旧 Owner/旧 epoch 的延迟 Patch 不覆盖新 Owner 快照；
- [x] Push 丢失或版本缺口时，重新进入后快照收敛。

说明：以上条目由 `server/internal/farmview`（`broadcast_test.go`）、
`server/internal/player`（`farm_view_notify_test.go`：买种不增加
`farm_view_seq`，种植/施肥/收获/清理各增加一次并广播）、
`server/internal/visit`（`owner_service_test.go` 的 `ListVisitors`）与
`server/internal/gateway`（`grpc_adapters_test.go` 的
`PublishFarmViewPatch` 参数校验、`push_hub.go` 的 `FARM_VIEW_CHANGED`
Envelope 校验）覆盖服务端行为；H5 侧的 epoch 变化/seq 缺口重新
`ENTER_FRIEND_FARM`、`seq == local + 1` 原地合并在
`web/src/App.vue`（`handleFarmViewChanged`）+ `web/src/lib/ws.ts`
（`FARM_VIEW_CHANGED` 分发校验）中实现，未写自动化 E2E（以
`npm run build` 类型检查 + 人工验证为主，与阶段 2 的访问会话验收方式
一致）。

### 预计涉及文件

新增：

- `server/internal/farmview/view.go`
- `server/internal/farmview/view_test.go`
- `server/internal/farmview/broadcast.go`
- `server/internal/farmview/broadcast_test.go`
- `server/internal/player/farm_view_notify_test.go`

修改：

- `server/internal/player/runtime.go`
- `server/internal/player/farm_visit_snapshot.go`
- `server/internal/player/grpc_push.go`
- `server/internal/visit/owner_service.go`
- `server/internal/visit/owner_service_test.go`
- `server/internal/gateway/grpc_push.go`
- `server/internal/gateway/push_hub.go`
- `server/internal/gateway/grpc_adapters_test.go`
- `server/cmd/gate/main.go`
- `server/cmd/zone/main.go`
- `web/src/lib/ws.ts`
- `web/src/App.vue`

说明：实际实现没有拆出独立的 `server/internal/farmview/push.go`；Gate
侧投递复用了阶段 3 已有的 `server/internal/gateway/grpc_push.go` 和
`push_hub.go`，Owner Zone 侧的推送客户端复用了阶段 1/3 已有的
`server/internal/player/grpc_push.go`（新增
`PublishFarmViewPatch` 方法），未新增/修改 Protobuf 契约（阶段 0 已
冻结 `FarmViewPatch`/`FARM_VIEW_CHANGED`/
`GatePushService.PublishFarmViewPatch`）。

## 4. 四种互动

- [ ] 访客投虫、捉虫、清理成功后各扣一次相应机会；
- [ ] 农场主自己捉虫、清理不扣机会；
- [ ] 同一块成长地不能同时存在两只虫；
- [ ] 虫源不能捉自己投下的虫；
- [x] 两名访客并发操作同一地块时，先进入农场主 Actor Mailbox 的请求成功
      （偷菜部分：`TestApplyStealOnOwnerConcurrentVisitorsRespectMaxStealTimes`；
      投虫/捉虫/清理并发仍待阶段 6）；
- [x] 只有符合冻结规则的成熟作物可以偷取，配置热更新不改变已种作物参数
      （`public_plot_view_test.go` 的 `CanSteal` 边界测试 +
      `plant_test.go`/`config_test.go` 的冻结值测试）；
- [x] 被偷次数、保护产量和单次偷取量均按冻结配置校验
      （`CanSteal`：`StealCount<Max`、`BaseYield>=StolenQuantity+
      StealQuantity+ProtectedOwnerYield`）；
- [x] 访客背包容量不足时不会修改农场主地块，也不会产生邮件兜底
      （`TestReserveStealRejectsOverStackLimit`、
      `TestReserveStealRejectsOverTypeLimitConsideringLiveReservations`：
      `ReserveSteal` 在容量校验失败时直接返回错误，从不进入
      `ApplyStealOnOwner`，因此无需邮件兜底路径）；
- [x] 偷取不满足条件时不增加 `stolen_quantity`，也不向访客发作物
      （`TestApplyStealOnOwnerRejectsWhenNotStealableWithoutMutating`）；
- [ ] 第二章偷菜、投虫任务只在对应访客 Saga 完成持久化后推进
      （偷菜部分已覆盖：`TestCommitStealCreditsInventoryAndTaskExactlyOnce`
      验证 `TASK_STEAL_CROP` 只在 `CommitSteal` 持久化提交后推进一次，
      重试不重复推进；投虫任务仍待阶段 6，本条目暂不整体勾选）。

### 预计涉及文件

新增：

- `server/internal/player/apply_pest.go`
- `server/internal/player/apply_pest_test.go`
- `server/internal/player/catch_pest.go`
- `server/internal/player/catch_pest_test.go`
- `server/internal/player/help_clean_plot.go`
- `server/internal/player/help_clean_plot_test.go`
- `server/internal/player/steal_crop.go`
- `server/internal/player/steal_crop_test.go`
- `server/internal/player/public_plot_view.go`
- `server/internal/player/plant_test.go`

修改：

- `proto/classicfarm/v1/data/data_model.proto`
- `proto/classicfarm/v1/ws/ws.proto`
- `proto/classicfarm/v1/friend/friend.proto`
- `deploy/tcaplus/schema/classicfarm/v1/tcaplus/friend_tables.proto`
- `server/internal/player/state.go`
- `server/internal/player/checkpoint.go`
- `server/internal/player/checkpoint_test.go`
- `server/internal/player/runtime.go`
- `server/internal/player/config.go`
- `server/internal/player/config_test.go`
- `server/internal/player/plant.go`
- `server/internal/player/clean_plot.go`
- `server/internal/player/growth.go`
- `server/cmd/zone/friend_rpc.go`
- `docs/contracts/data-model.md`
- `docs/contracts/idempotency-and-errors.md`
- 生成目录：`server/gen/classicfarm/v1/data/`
- 生成目录：`server/gen/classicfarm/v1/ws/`
- 生成目录：`server/gen/classicfarm/v1/friend/`
- 生成目录：`server/gen/classicfarm/v1/tcaplus/`
- 生成目录：`web/src/gen/classicfarm/v1/`

## 5. Saga 恢复

（以下条目目前仅覆盖偷菜一种互动；投虫/捉虫/清理的 Saga 集成在阶段 6
基于同一套 `server/internal/interaction` 基础设施补齐。）

- [x] 相同 `interaction_id` 重试不会重复扣机会、加作物或改地块
      （`TestStealSagaHappyPathCompletesExactlyOnce`、
      `TestApplyStealOnOwnerMutatesOnceAndDedupesRetry`、
      `TestCommitStealCreditsInventoryAndTaskExactlyOnce`）；
- [x] `VISITOR_RESERVED` 阶段中断后可释放预留
      （`TestReleaseStealReleasesLiveReservationIdempotently`、
      `TestStealSagaDeterministicOwnerRejectionReleasesAndAborts`）；
- [x] 访客 Checkpoint 已保存但 Saga 尚未标记 `VISITOR_RESERVED` 时可对账
      （崩溃窗口 A：`TestStealSagaRecoversCrashWindowA`）；
- [x] `OWNER_APPLIED` 阶段中断后可从农场主回执继续访客提交
      （崩溃窗口 B：`TestStealSagaRecoversCrashWindowB`）；
- [x] 农场主 Checkpoint 已保存但 Saga 尚未标记 `OWNER_APPLIED` 时可对账
      （同崩溃窗口 B：owner 回执先于 Saga 记录持久化，`Reconciler`/重试
      通过回执去重恢复）；
- [x] `VISITOR_COMMITTED` 阶段中断后重试返回相同完成结果
      （崩溃窗口 C：`TestStealSagaRecoversCrashWindowC`）；
- [x] Tcaplus CAS 冲突、重试性失败、旧副本和 Fence 拒绝均不会误判完成
      （`server/internal/interaction/tcaplus_store_test.go` 的 CAS 冲突
      测试 + `TestStealSagaTransportFailureDoesNotBecomeSuccess`（`flakyStore`
      模拟传输失败），失败路径均返回错误而非伪造成功）；
- [x] Saga 同步 SaveCAS 不改变普通单玩家命令的异步 Dirty 语义
      （`TestOrdinaryCommandRemainsAsyncDirtyAfterSyncInteraction`）；
- [x] 未完成 Interaction 会被 Actor 激活或 Zone 定时任务对账
      （`server/internal/interaction/reconciler_test.go`；
      `server/cmd/zone/main.go` 以 5 秒 ticker 驱动
      `Reconciler.ReconcileDue`）；
- [x] 终态 Interaction 保留 24 小时，未完成记录不自动删除
      （`Store`/`TcaplusStore`/`MemoryStore` 均无删除方法，终态记录只
      通过状态字段区分，不做主动过期清理——与 Phase 0 冻结的
      `FriendInteraction` 表结构一致；24 小时留存本身依赖 Tcaplus 的
      TTL/运维策略，不在本阶段代码验证范围内）。

### 预计涉及文件

新增：

- `server/internal/interaction/saga.go`
- `server/internal/interaction/store.go`
- `server/internal/interaction/tcaplus_store.go`
- `server/internal/interaction/reconciler.go`
- `server/internal/interaction/saga_test.go`
- `server/internal/interaction/tcaplus_store_test.go`
- `server/internal/interaction/reconciler_test.go`

修改：

- `deploy/tcaplus/schema/classicfarm/v1/tcaplus/friend_tables.proto`
- `proto/classicfarm/v1/data/data_model.proto`
- `server/internal/player/state.go`
- `server/internal/player/checkpoint.go`
- `server/internal/player/checkpoint_store.go`
- `server/internal/player/tcaplus_durable_store.go`
- `server/internal/player/tcaplus_store.go`
- `server/internal/player/runtime.go`
- `server/cmd/zone/main.go`
- `server/cmd/zone/friend_rpc.go`
- `.env.example`
- `deploy/k8s/configmap.yaml`
- `deploy/tcaplus/README.zh-CN.md`
- `docs/contracts/data-model.md`
- `docs/contracts/idempotency-and-errors.md`
- 生成目录：`server/gen/classicfarm/v1/data/`
- 生成目录：`server/gen/classicfarm/v1/tcaplus/`

## 6. 端到端演示

- [x] 两名玩家建立好友；
- [x] 访客进入农场主农场，农场主看到进入提示；
- [x] 两个访客同时查看农场主农场；
- [x] 一名访客完成投虫、捉虫、清理和偷取；
- [x] 三个 H5 的公开地块视图一致；
- [x] 访客离开或超时后，农场主不再向其下发公开 Patch；
- [x] 完整停止并恢复服务后，好友关系和 Saga 终态保持正确。

验收证据：`docs/evidence/2026-08-07-friend-interaction-e2e.md`
（`TestFriendInteraction` + `TestFriendRestartRecovery`）。

### 预计涉及文件

新增：

- `server/test/e2e/friend_interaction_test.go`
- `server/test/e2e/friend_interaction_saga_recovery_test.go`
- `tests/e2e/run-friend-interaction.sh`
- `tests/e2e/run-friend-restart-recovery.sh`
- `docs/evidence/<日期>-friend-interaction-e2e.md`

修改：

- `start-servers.sh`
- `Dockerfile`
- `deploy/k8s/configmap.yaml`
- `deploy/k8s/services.yaml`
- `deploy/k8s/kustomization.yaml`
- `deploy/k8s/gate.yaml`
- `deploy/k8s/zone.yaml`
- `deploy/k8s/friend.yaml`
- `deploy/k8s/README.zh-CN.md`
- `deploy/tcaplus/README.zh-CN.md`
- `tests/catalog.json`
- `web/src/main.ts`
- `web/src/lib/ws.ts`
- `docs/context/CURRENT.md`
