# AI Work Record: H5 farm loop

## Goal

Replace the snapshot-only proof page with a demonstrable farm interface for the
complete accepted owner loop.

## Changes

- added typed H5 requests for `GET_SHOP` and all seven write commands;
- centralized response version checks, patch application, business-error
  messages and gap recovery;
- added a responsive farm dashboard for shop, plot, inventory and chapter
  tasks;
- mapped control availability only from authoritative snapshot and quote state;
- added server-time-based maturity countdown while retaining Push/snapshot as
  final authority;
- reused the project-owned placeholder art for plots, crops, items and effects;
- retained connection and Actor diagnostics as a secondary defense aid.

## Verification

- `npm run typecheck`: pass;
- `npm run build`: pass;
- a browser-driven in-memory run completed every action through
  `state_version=1/8`, consumed one maturity Push and required no gap recovery;
- the final view showed the expected 29 coins, inventory, chapter two and empty
  plot;
- a 320 CSS-pixel device check found no horizontal overflow.

Observed output is in `../evidence/2026-07-31-h5-farm-loop-browser.md`.

## Remaining uncertainty

The browser flow has not yet been repeated with MySQL persistence. Final art,
broader accessibility review, error-path UX, production Push delivery and
capacity remain outside this slice.
